<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PhonePe Payment</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --phonepe-purple: #5f259f;
      --phonepe-dark: #4a1e81;
      --light-bg: #f8f9fa;
    }
    
    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--light-bg);
      color: #333;
    }
    
    h1, h2, h3, h4, h5, h6 {
      font-family: 'Poppins', sans-serif;
      font-weight: 600;
    }
    
    .payment-card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .payment-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.12);
    }
    
    .payment-header {
      background: linear-gradient(135deg, var(--phonepe-purple), var(--phonepe-dark));
      color: white;
      padding: 1.5rem;
      text-align: center;
    }
    
    .payment-body {
      padding: 2rem;
      background-color: white;
    }
    
    .form-label {
      font-weight: 500;
      color: #555;
      margin-bottom: 0.5rem;
    }
    
    .form-control {
      padding: 0.75rem 1rem;
      border-radius: 10px;
      border: 1px solid #e0e0e0;
      transition: all 0.3s ease;
    }
    
    .form-control:focus {
      border-color: var(--phonepe-purple);
      box-shadow: 0 0 0 0.25rem rgba(95, 37, 159, 0.2);
    }
    
    .btn-payment {
      background-color: var(--phonepe-purple);
      border: none;
      border-radius: 10px;
      padding: 0.75rem;
      font-weight: 500;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
    }
    
    .btn-payment:hover {
      background-color: var(--phonepe-dark);
      transform: translateY(-2px);
    }
    
    .secure-note {
      font-size: 0.85rem;
      color: #777;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    
    .phonepe-logo {
      height: 50px;
      filter: brightness(0) invert(1);
    }
    
    @media (max-width: 576px) {
      .payment-body {
        padding: 1.5rem;
      }
      
      .payment-card {
        border-radius: 12px;
      }
    }
  </style>
</head>

<body>
  <div class="container py-5">
    <div class="payment-card mx-auto" style="max-width: 500px;">
      <div class="payment-header">
        <img src="/images/phonepe-logo.png" alt="PhonePe Logo" class="phonepe-logo mb-2"/>
        <h4 class="mb-0">Pay with PhonePe</h4>
      </div>
      
      <div class="payment-body">
        <form action="/pay" method="POST">
          <div class="mb-4">
            <label class="form-label">Name</label>
            <input type="text" name="name" class="form-control" placeholder="Your Name" required>
          </div>

          <div class="mb-4">
            <label class="form-label">Phone Number</label>
            <div class="input-group">
              <span class="input-group-text bg-light">+91</span>
              <input type="tel" name="phone" class="form-control" placeholder="10-digit mobile number" required pattern="[0-9]{10}">
            </div>
          </div>

          <div class="mb-4">
            <label class="form-label">Amount (INR)</label>
            <div class="input-group">
              <span class="input-group-text bg-light">₹</span>
              <input type="number" name="amount" class="form-control" placeholder="Enter amount" required min="1">
            </div>
          </div>

          <button type="submit" class="btn btn-payment w-100 py-3 mb-3">
            <i class="fas fa-lock me-2"></i> Proceed to Pay
          </button>

          <div class="secure-note">
            <i class="fas fa-shield-alt"></i>
            <span>Secure payment powered by PhonePe</span>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>